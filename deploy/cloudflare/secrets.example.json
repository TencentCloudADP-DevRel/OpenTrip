{
  "_comment": "Key names only — never commit real values. Set with `wrangler secret put <KEY> --config wrangler.api.jsonc`.",
  "BETTER_AUTH_SECRET": "<32+ char random string, e.g. `openssl rand -base64 32`>"
}
